import type { ContentFile } from "../types";

const bindiProjects: ContentFile[] = [
  {
    name: "coming-soon.md",
    content: `## Coming soon

Philosophy builds no repos. It only questions the ones that exist.`,
  },
];

export default bindiProjects;
