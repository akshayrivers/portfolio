import type { ContentFile } from "../types";

const bindiMemories: ContentFile[] = [
  {
    name: "coming-soon.md",
    content: `## Coming soon

If true death is oblivion, then memories are the only afterlife on offer. Collection in progress.`,
  },
];

export default bindiMemories;
